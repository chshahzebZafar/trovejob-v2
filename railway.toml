[build]
builder = "nixpacks"
buildCommand = "npm install --prefix packages/db && npx --prefix packages/db prisma generate && npm install --prefix apps/api"

[deploy]
startCommand = "sh -c 'cd apps/api && npx tsx src/index.ts'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
