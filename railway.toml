[build]
builder = "nixpacks"
buildCommand = "cd packages/db && npx prisma generate"

[deploy]
startCommand = "sh -c 'cd apps/api && npx tsx src/index.ts'"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
